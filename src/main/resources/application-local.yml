spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tripio
    username: tripio
    password: tripio
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
